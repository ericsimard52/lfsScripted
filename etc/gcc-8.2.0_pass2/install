processCmd "make install"
[ $? -gt 0 ] && return 1

log "PKG|INFO|Linking gcc to /tools/bin/cc" t
[ -L /tools/bin/cc ] && processCmd "rm -v /tools/bin/cc"
processCmd "ln -sv gcc /tools/bin/cc"

return 0
