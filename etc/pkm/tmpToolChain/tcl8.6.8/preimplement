pushd unix
  chmod -v u+w /tools/lib/libtcl8.6.so
if [[ $? > 0 ]]; then
    return 1
fi
make install-private-headers
if [[ $? > 0 ]]; then
    return 1
fi
ln -sv tclsh8.6 /tools/bin/tclsh
if [[ $? > 0 ]]; then
    return 1
fi
popd
  