make DESTDIR=$FAKEROOT/$sdn install
mkdir $FAKEROOT/$sdn/{bin,lib}
mv -v   $FAKEROOT/$sdn/usr/bin/{lzma,unlzma,lzcat,xz,unxz,xzcat} $FAKEROOT/$sdn/bin
mv -v $FAKEROOT/$sdn/usr/lib/liblzma.so.* $FAKEROOT/$sdn/lib

