processCmd "./configure --prefix=/tools --without-python --disable-makeinstall-chown --without-systemdsystemunitdir --without-ncurses PKG_CONFIG=\"\""
if [[ $? > 0 ]]; then
    log "PKG|ERROR|Error with configure." t
    return 1
fi
return 0
